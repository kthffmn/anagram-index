# Anagram Index Technical Interview

## Running Tests

To run the tests, clone down the repo, cd into it, then run rspec:

```bash
$ git clone https://github.com/kthffmn/anagram-index.git
$ cd anagram-index
$ rspec
```